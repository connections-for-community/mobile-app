# Will migrate to a proper security documentation tab (future)

Expo-secure-store: Make login token secure so that even though user will automatically login to app (if already logged in) when on phone, other apps cannot access this data.